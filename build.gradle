plugins {
    id "org.featurea" version "$featurea_version"
}

artifact "test"

source {
    include "org.featurea:loader:$featurea_version"
    include "org.featurea:graphics:$featurea_version"
    include "org.featurea:window:$featurea_version"
}

examples {
    mainCall("level1")
}
