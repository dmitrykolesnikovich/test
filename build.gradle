buildscript {
    repositories {
        mavenLocal()
    }
    dependencies {
        classpath "org.featurea:gradle:0.0.4"
    }
}

apply plugin: "org.featurea.gradle"

artifact "test"

source {
    include "org.featurea:window:$featurea_version"
    include "org.featurea:graphics:$featurea_version"
}

examples {
    mainCall("level1")
}
